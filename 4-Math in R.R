#MATH

#Like many other languages, we can use basic operators in R.

10+5
14-5
20/3
22*5

#R also has built in functions
min(15, 5, 12, 51, 23)
max(15, 5, 12, 51, 23)

sqrt(4)#Square root

abs(-12)#absolute value

ceiling(1.4)#rounds the number up to the nearest integer
floor(1.4)#rounds the number down to the nearest integer